# Otus Diplom

Тема проекта: создание Telegram-бота для организации отчетности сотрудников перед lead.

## Роли пользователей бота

В Telegram-боте будут использоваться следующие роли.

### Сотрудник

Сотрудник - основной пользователь бота, который отправляет отчетность о своей работе.

Функции сотрудника:

- отправка ежедневного отчета;
- указание выполненных задач;
- указание текущих проблем или блокеров;
- просмотр своего последнего отправленного отчета;
- просмотр списка назначенных задач;
- перевод задачи в статус "в работе";
- закрытие задачи с обязательным комментарием;
- получение уведомления о назначенной задаче;
- получение уведомления о скором окончании срока задачи;
- получение напоминаний о необходимости отправить отчет.

### Lead

Lead - пользователь, который контролирует отчетность сотрудников и просматривает результаты их работы.

Функции lead:

- просмотр отчетов сотрудников;
- просмотр отчета конкретного сотрудника;
- назначение задач сотрудникам;
- указание срока выполнения задачи;
- просмотр списка задач конкретного сотрудника;
- просмотр статусов задач сотрудника;
- просмотр списка сотрудников;
- проверка, кто отправил отчет за выбранный день;
- проверка, кто еще не отправил отчет;
- получение краткой сводки по отчетам команды;
- получение списка задач всей группы.

### Администратор

Администратор - пользователь, который помогает настраивать работу бота.

Функции администратора:

- добавление сотрудников в систему;
- назначение роли lead;
- удаление пользователей из системы;
- просмотр списка зарегистрированных пользователей;
- настройка базовых параметров бота;
- настройка времени уведомления до окончания срока задачи.

## Статусы задач

В боте используются простые статусы задач:

- открыто - задача создана и назначена сотруднику;
- в работе - сотрудник начал выполнять задачу;
- закрыто - сотрудник завершил задачу и оставил комментарий.

Новая задача всегда создается со статусом "открыто".

## MVP проекта

Минимальный набор ролей, функций и запросов для первой версии Telegram-бота.

На текущем этапе MVP реализован как консольное приложение. Вместо базы данных используются списки `List<>`, а вместо подключения к Telegram пользователь вводит команды в консоль.

### Роли MVP

В MVP остаются две основные роли:

- сотрудник;
- lead.

Роль администратора можно добавить позже, потому что для первой версии настройки можно хранить в коде или конфигурации проекта.

### Функции MVP

Функции сотрудника:

- отправить ежедневный отчет;
- добавить выполненную задачу в отчет;
- указать проблему или блокер;
- посмотреть свои назначенные задачи;
- перевести задачу в работу;
- закрыть задачу с комментарием.

Функции lead:

- посмотреть отчеты сотрудников;
- посмотреть отчет конкретного сотрудника;
- назначить задачу сотруднику со сроком;
- посмотреть задачи конкретного сотрудника;
- посмотреть задачи всей группы.

### Запросы MVP

Запросы сотрудника:

- `/report` - отправить ежедневный отчет;
- `/task` - добавить выполненную задачу;
- `/block` - указать проблему или блокер;
- `/my_tasks` - посмотреть свои задачи;
- `/start_task 1` - перевести задачу в работу;
- `/close_task 1` - закрыть задачу с комментарием.

Запросы lead:

- `/reports` - посмотреть отчеты сотрудников за сегодня;
- `/employee_report Иван Иванов` - посмотреть отчет конкретного сотрудника;
- `/assign_task Иван Иванов | Подготовить отчет | 20.05.2026` - назначить задачу сотруднику;
- `/employee_tasks Иван Иванов` - посмотреть задачи конкретного сотрудника;
- `/team_tasks` - посмотреть задачи всей группы.

### Запуск MVP

Для запуска консольного прототипа:

```text
dotnet run --project Otus_diplom/Otus_diplom.csproj
```

В приложении есть тестовые пользователи:

- Иван Иванов - сотрудник;
- Петр Петров - сотрудник;
- Анна Lead - lead.

Для переключения пользователя используется команда:

```text
/login Иван Иванов
```

Также можно войти по номеру пользователя:

```text
/login 3
```

## Архитектура реализации

Проект разделен на слои по архитектурному промту.

### Core

Папка `Core` содержит доменные сущности, интерфейсы репозиториев и бизнес-сервисы.

Основные части:

- `Core/Entities` - сущности `User`, `DailyReport`, `EmployeeTask` и enum-статусы;
- `Core/DataAccess` - интерфейсы репозиториев `IUserRepository`, `IReportRepository`, `ITaskRepository`;
- `Core/Services` - интерфейсы и реализации бизнес-сервисов `IReportService`, `ReportService`, `ITaskService`, `TaskService`.

Core не зависит от консоли, Telegram API или конкретной базы данных.

### Infrastructure

Папка `Infrastructure` содержит реализации хранения данных.

Сейчас есть две группы классов:

- `InMemoryUserRepository`, `InMemoryReportRepository`, `InMemoryTaskRepository` - рабочие реализации через `List<>`;
- `SqlUserRepository`, `SqlReportRepository`, `SqlTaskRepository` - заготовки для будущей работы с базой данных.

Также добавлены:

- `IDataContextFactory` и `DataContextFactory` - заготовка фабрики подключения к БД;
- `DataAccess/Models` - модели будущих таблиц БД;
- `ModelMapper` - преобразование моделей БД в доменные сущности и обратно.

### TelegramBot

Папка `TelegramBot` содержит интерфейс отправки сообщений и заготовку Telegram-слоя.

Основные части:

- `IMessageSender` - общий интерфейс отправки сообщений;
- `ConsoleMessageSender` - текущая отправка сообщений через консоль;
- `TelegramApiMessageSender` - заготовка отправки сообщений через Telegram API;
- `UpdateHandler` - заготовка обработчика Telegram-команд.

За счет интерфейса `IMessageSender` консольную отправку можно заменить на Telegram API без изменения бизнес-сервисов.

### Переключение хранилища

По умолчанию приложение использует хранение в памяти.

Для будущего SQL-хранилища предусмотрены переменные окружения:

```text
DATA_PROVIDER=sql
DB_CONNECTION_STRING=строка_подключения
```

SQL-репозитории сейчас являются учебными заготовками. Полная работа с реальной БД будет добавляться отдельно.

## Запросы и ответы бота

Ниже описаны основные запросы пользователя и ответы Telegram-бота для функций из раздела с ролями.

### Сотрудник

#### Отправка ежедневного отчета

Запрос:

```text
/report
```

Ответ:

```text
Отправить отчет?
```
Запрос:

```text
Да
```

Бот отвечает если "Да":

```text
Отчет отправлен.
```
В базе данных ставится пометка, что сотрудник за сегодня сдал отчет. 

Бот отвечает если "Нет":

После завершения:

```text
Отчет не отправлен.
```

#### Указание выполненных задач

Запрос:

```text
/task
```

Ответ:

```text
Введите выполненную задачу.
```

Запрос:

```text
Сделал задачу по авторизации, исправил ошибку в форме отчета.
```

Ответ:

```text
Выполненные задачи добавлены в отчет.
```

#### Указание проблем или блокеров

Запрос:

```text
/block
```

Ответ:
```text
Какая проблема возникла?
```

Запрос
```text
Проблема с доступом.
```

Ответ:

```text
Проблемы добавлены в отчет.
```

#### Просмотр последнего отчета

Запрос:

```text
/my_last_report
```

Ответ:

```text
Ваш последний отчет:
Дата: 16.05.2026
Выполнено: сделал задачу по авторизации.
Проблемы: нет.
```

#### Получение напоминания

Запрос выполняется ботом автоматически.

Ответ:

```text
Напоминание: не забудьте отправить ежедневный отчет.
```

#### Просмотр списка назначенных задач

Запрос:

```text
/my_tasks
```

Ответ:

```text
Ваши задачи:
1. Подготовить отчет по проекту
Статус: открыто
Срок: 20.05.2026

2. Проверить форму отправки отчета
Статус: в работе
Срок: 18.05.2026
```

#### Перевод задачи в работу

Запрос:

```text
/start_task 1
```

Ответ:

```text
Задача "Подготовить отчет по проекту" переведена в статус "в работе".
```

#### Закрытие задачи

Запрос:

```text
/close_task 1
```

Ответ:

```text
Введите комментарий по выполненной задаче.
```

После ввода комментария:

```text
Задача закрыта. Комментарий сохранен.
```

Если сотрудник не оставил комментарий:

```text
Для закрытия задачи необходимо оставить комментарий.
```

#### Уведомление о назначенной задаче

Запрос выполняется ботом автоматически после назначения задачи lead.

Ответ:

```text
Вам назначена новая задача:
Подготовить отчет по проекту
Срок: 20.05.2026
Статус: открыто
```

#### Уведомление о скором окончании срока задачи

Запрос выполняется ботом автоматически за время, которое настроил администратор.

Ответ:

```text
Напоминание: срок задачи "Подготовить отчет по проекту" заканчивается 20.05.2026.
```

### Lead

#### Просмотр отчетов сотрудников

Запрос:

```text
/reports
```

Ответ:

```text
Отчеты сотрудников за сегодня:
1. Иван Иванов - отчет отправлен.
2. Петр Петров - отчет не отправлен.
```

#### Просмотр отчета конкретного сотрудника

Запрос:

```text
/employee_report Иван Иванов
```

Ответ:

```text
Отчет сотрудника Иван Иванов за сегодня:
Выполнено: сделал задачу по авторизации.
Проблемы: нет.
```

#### Назначение задачи сотруднику

Запрос:

```text
/assign_task Иван Иванов | Подготовить отчет по проекту | 20.05.2026
```

Ответ lead:

```text
Задача назначена сотруднику Иван Иванов.
Статус задачи: открыто.
Срок: 20.05.2026.
```

Ответ сотруднику:

```text
Вам назначена новая задача:
Подготовить отчет по проекту
Срок: 20.05.2026
Статус: открыто
```

#### Просмотр задач конкретного сотрудника

Запрос:

```text
/employee_tasks Иван Иванов
```

Ответ:

```text
Задачи сотрудника Иван Иванов:
1. Подготовить отчет по проекту
Статус: открыто
Срок: 20.05.2026

2. Проверить форму отправки отчета
Статус: в работе
Срок: 18.05.2026

3. Обновить инструкцию
Статус: закрыто
Срок: 15.05.2026
Комментарий: инструкция обновлена и отправлена lead.
```

#### Просмотр списка сотрудников

Запрос:

```text
/employees
```

Ответ:

```text
Список сотрудников:
1. Иван Иванов
2. Петр Петров
```

#### Проверка, кто отправил отчет за выбранный день

Запрос:

```text
/reports 16.05.2026
```

Ответ:

```text
Отчет за 16.05.2026 отправили:
1. Иван Иванов
```

#### Проверка, кто не отправил отчет

Запрос:

```text
/missing_reports
```

Ответ:

```text
Сегодня отчет еще не отправили:
1. Петр Петров
```

#### Получение краткой сводки по команде

Запрос:

```text
/summary
```

Ответ:

```text
Сводка по команде за сегодня:
Всего сотрудников: 2
Отчеты отправили: 1
Отчеты не отправили: 1
Проблемы указали: 0
```

#### Получение списка задач всей группы

Запрос:

```text
/team_tasks
```

Ответ:

```text
Задачи всей группы:

Иван Иванов:
1. Подготовить отчет по проекту
Статус: открыто
Срок: 20.05.2026

Петр Петров:
1. Проверить форму отправки отчета
Статус: в работе
Срок: 18.05.2026
```

### Администратор

#### Добавление сотрудника

Запрос:

```text
/add_employee Иван Иванов
```

Ответ:

```text
Сотрудник Иван Иванов добавлен.
```

#### Назначение роли lead

Запрос:

```text
/set_lead Иван Иванов
```

Ответ:

```text
Пользователю Иван Иванов назначена роль lead.
```

#### Удаление пользователя

Запрос:

```text
/remove_user Иван Иванов
```

Ответ:

```text
Пользователь Иван Иванов удален.
```

#### Просмотр списка зарегистрированных пользователей

Запрос:

```text
/users
```

Ответ:

```text
Зарегистрированные пользователи:
1. Иван Иванов - сотрудник
2. Петр Петров - lead
```

#### Настройка базовых параметров бота

Запрос:

```text
/settings
```

Ответ:

```text
Настройки бота:
Время напоминания: 18:00
Рабочие дни: понедельник - пятница
```

#### Настройка времени уведомления до окончания срока задачи

Запрос:

```text
/set_task_deadline_reminder 24
```

Ответ:

```text
Уведомление о скором окончании срока задачи будет отправляться за 24 часа до срока.
```
